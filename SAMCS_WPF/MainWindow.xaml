<Window
    x:Class="SAMCS_WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:SAMCS_WPF.ViewModels"
    Title="SAMCS 硬件连接测试"
    Width="600"
    Height="400"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <d:Window.DataContext>
        <vm:MainViewModel />
    </d:Window.DataContext>

    <Grid Background="#F5F6FA">
        <StackPanel VerticalAlignment="Top" Margin="20,20,20,0">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="通信端口:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding ComPort}" Width="100" Height="30" FontSize="16" 
                 VerticalContentAlignment="Center" Padding="5,0"/>
            </StackPanel>

            <TextBlock
                Margin="0,0,0,20"
                HorizontalAlignment="Center"
                FontSize="18"
                FontWeight="Medium"
                Foreground="#2D3436"
                Text="{Binding ConnectionStatus}" />

            <TextBlock
                Margin="0,0,0,20"
                HorizontalAlignment="Center"
                FontSize="14"
                Foreground="#636E72"
                Text="{Binding AxisPositions}"
                TextWrapping="Wrap" />

            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Button
                    Width="140"
                    Height="45"
                    Margin="0,0,20,0"
                    Background="#0984E3"
                    BorderThickness="0"
                    Command="{Binding ConnectHardwareCommand}"
                    Content="🔗 连接硬件"
                    FontSize="16"
                    Foreground="White">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5" />
                        </Style>
                    </Button.Resources>
                </Button>

                <Button
                    Width="140"
                    Height="45"
                    Background="#D63031"
                    BorderThickness="0"
                    Command="{Binding DisconnectHardwareCommand}"
                    Content="🛑 断开通信"
                    FontSize="16"
                    Foreground="White">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5" />
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>